// flags: --experimental-laws
// error: constraint `Invalid` cannot call `print`

component Position { x: int = 0 }
constraint Invalid for Position(subject, proposed) {
    print(proposed.x)
    require true else "invalid.effect"
}
