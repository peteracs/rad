fn main() -> nil {
    let t0 = clock()
    let mut sum = 0
    let mut i = 0
    while i < 1000000 {
        sum = sum + i
        i = i + 1
    }
    let t1 = clock()
    let elapsed = t1 - t0
    print(f"sum={sum}")
    if elapsed > 0.0 {
        print("clock works")
    } else {
        print("clock broken")
    }
}
