// backend: both
// Mixed map values should widen to map<*, any>, allowing dynamic arithmetic.
let model = {
    "weights": [1.0, 2.0, 3.0],
    "final_loss": 0.1417
}

let scaled = model["final_loss"] * 10000
print(int(scaled))
// expect: 1417
