// backend: both
component A { x: 0 }

component B { y: 0 }

fn main() -> nil {
    let e = spawn(A { x: 1 })
    let f = fork()

    print(int(is_some(peek(f, e, A))))
    print(int(is_none(peek(f, e, B))))
}
// expect: 1
// expect: 1
