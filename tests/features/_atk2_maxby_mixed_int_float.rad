// Attack: max_by where key fn returns mix of int and float across elements
fn main() -> nil {
    let data = [1, 2.5, 3, 1.5]
    let result = max_by(data, fn(x) { return x })
    match result {
        Some(v) => { print("max:", v) }
        None => { print("none") }
    }
}

// expect: max: 3
