fn main() -> nil {
    let pairs = [[1, 1], [2, 3], [4, 4], [5, 6]]
    let equal_pairs = pairs |> filter(fn([a, b]) { return a == b })
    print(len(equal_pairs))
    for [x, y] in equal_pairs {
        print(f"{x}=={y}")
    }
}
