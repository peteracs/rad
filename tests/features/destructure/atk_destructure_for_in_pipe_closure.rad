fn main() -> nil {
    let groups = [[[1, 2], [3, 4]], [[5, 6]]]
    let sums = groups |> map(fn(group) {
        let mut s = 0
        for [a, b] in group {
            s = s + a + b
        }
        return s
    })
    print(sums[0])
    print(sums[1])
}
