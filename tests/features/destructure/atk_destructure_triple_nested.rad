fn main() -> nil {
    let data = [[(1, 2), (3, 4)], [(5, 6), (7, 8)]]
    let mut grand_total = 0
    for [pair_a, pair_b] in data {
        let row_pairs = [pair_a, pair_b]
        for [x, y] in row_pairs {
            grand_total = grand_total + x + y
        }
    }
    print(grand_total)
}
