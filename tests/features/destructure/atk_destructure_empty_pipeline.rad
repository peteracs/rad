fn main() -> nil {
    let rows: list = []
    let out = rows
        |> filter(fn([a, b]) { return a > 0 })
        |> map(fn([a, b]) { return a + b })
    print(len(out))
    print("done_empty_pipe")
}
